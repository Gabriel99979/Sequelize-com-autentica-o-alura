// Vamos fazer um loggin de segurança
// Qualquer pessoa que tem acesso ao nosso projeto vai poder acessar diretamente o que não é desejado
// Vamos criar o authRoutes para termos nossas rotas de segurança loggin, cadastro entre outras

// const { Router } = require('express');

// const router = Router();

// router
//     .post('/auth/login')

// module.exports = router

// Vamos agora no index e importar as rotas de loggin

// Após isso vamos criar o nosso controller de authcontroller.js
// Vamos criar o service que é authService.js

// Podemos verificar a parte de verificação da nossa senha

// Vamos baixar o JWT(jsonwebtoken)

// npm i jsonwebtoken